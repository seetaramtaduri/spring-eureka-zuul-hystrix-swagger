spring:
  application:
    name: eureka-server

server:
  port: 8761

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
       defaultZone: http://localhost:8761/eureka
    server:
      waitTimeInMsWhenSyncEmpty: 0

endpoints:
  sensitive: false
